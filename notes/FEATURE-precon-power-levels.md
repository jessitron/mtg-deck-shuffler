# possible feature. Note power levels of each precon on deck selection.

Deckcheck.co has some rankings that are easy to retrieve. We could grab them when we download decks and record them.

See example curl in `notes/example-curl.txt`.
and sample response in `notes/example-response.json`.
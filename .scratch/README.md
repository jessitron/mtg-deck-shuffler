# .scratch — the tracker

This is where committed work lives: one directory per feature, `spec.md` plus
`issues/NN-slug.md`. It is **committed on purpose** — it's the tracker, not scratch space, and
git is what carries it between Jess's computers.

The conventions (header block, `Mountain:`, `Status:`) are in `docs/agents/issue-tracker.md`.
Pre-decision captures go in `TODO.md` at the repo root instead.

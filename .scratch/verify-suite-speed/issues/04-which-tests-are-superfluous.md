# Decide which verification tests are superfluous

Mountain: overhead
Status: resolved
Type: grilling
Map: ../map.md

> _This was generated by AI during triage._

Tickets 02 and 03 make the suite **faster**. This one asks the other question: which of these
tests should exist at all? A test that is redundant, vacuous, or asserting something nothing
depends on costs time on every run forever and gives nothing back.

## Why this can't go to an agent

Deleting a test is a decision about what the Shuffler must never break. An agent can gather the
evidence and make a case; only Jess can say "we don't need to keep proving that."

## Evidence already in hand

**A vacuous assertion, found while implementing 02.** In `verify-library-grouping.spec.ts`, both
flip loops click the flip button and then assert:

```ts
await expect(positionIndicator).toHaveText(`Card ${currentPosition} of ${totalInGroup}`);
```

That is the *same text as before the flip* — which is exactly the property under test
("flipping preserves group-scoped navigation"), but it means **a flip that never happened
passes too.** The test cannot distinguish success from the button doing nothing. It is
currently a comment in the spec pointing here.

Question for this ticket: strengthen it (assert the face actually changed — the back face has a
different card name) or drop it?

**Two near-duplicate flip loops.** `verify-library-grouping.spec.ts` has one for the game page
and one for the prep page, ~60 lines each, differing only in route. Both iterate every card in a
type group looking for a two-faced one. Is the prep/game distinction load-bearing here, or is
one enough?

**Nine query-parameter tests in one file.** `verify-query-parameter-modals.spec.ts` covers
`?openCard`, `?openLibrary`, `?openTable`, `?openHistory`, `?openDebug`, three combinations, and
two "no parameters" baselines — for both game and prep pages. This is the largest spec and one
of the slowest. All nine exercise the same mechanism (`public/modal-query-params.js` reading
`URLSearchParams` and firing an htmx request). Does each parameter need its own end-to-end
browser test, or does the mechanism need one and the parameter table a unit test?

## What would make this decidable

For each spec, three questions:

1. **What would break in front of a player if this test vanished?** If the answer is "nothing we
   couldn't catch elsewhere", it's a candidate.
2. **Is it testing the mechanism or the table?** Nine near-identical cases over one mechanism is
   a parameterization, and parameterizations are cheaper as unit tests.
3. **Has it ever failed for a real reason?** Git history knows. The three table-mode specs that
   were broken for weeks (fixed 2026-08-07) are evidence *for* keeping tests that catch real
   regressions — and evidence about which ones do.

The suite now traces itself (`mtg-fleet-verify`, ticket 01), so "which specs cost the most" is a
query rather than a guess. Cost per spec plus value per spec is the whole decision.

## Sequencing

Do **after 02** — 02 changes the cost side of the ledger substantially, and a test that looked
too expensive at 28s may be fine at 8s. Deciding before then would delete tests for the wrong
reason.

## Related

- `02-optimize-the-suite.md` — makes the tests faster; found the vacuous assertion above.
- `03-setup-cost-and-isolation.md` — the other cost-side ticket.
- `set-up-ci` in `TODO.md` — CI pays this cost per push, which sharpens every question here.

## Comments

### Created, 2026-08-07

> _This was generated by AI during triage._

Asked for by Jess while ticket 02 was being implemented. Seeded with the three concrete
candidates that surfaced during 02's sweep rather than left as a bare "review the tests" line —
the vacuous flip assertion in particular was found by reading every wait site in the suite.

### From the two-faced-cards owner, 2026-08-07

> _This was generated by AI during triage._

Asked whether face state is observable at all, so the vacuous assertion above could be
strengthened rather than argued about. It went and read the code. The answer is specific:

- **Inline surfaces: observable, cleanly.** `.flip-container-outer` gets `.card-flipped` when
  `currentFace === "back"` (`src/view/common/shared-components.ts:104`), server-rendered, true
  the instant the swap settles. This is why `verify-prep-commander-flip.spec.ts` is a sound test
  and the grouping ones aren't.
- **The card modal: not observable.** No class, no attribute, no face-dependent text. A DFC's
  `card.name` is the whole `"Front // Back"` string, so the modal title and `alt` are byte-
  identical on both faces. `views/partials/card-modal.ejs` *receives* `currentFace` and never
  renders it.
- Two indirect observables: `img.modal-card-image`'s `src` (different Scryfall files per face),
  on both pages; and **on prep only**, the flip button's `hx-get=".../?face=<target>"`. The
  **game** modal's flip button is a `hx-post` to a toggling route, byte-identical on both faces
  — useless for assertion.

**So there's a third option beyond strengthen-or-drop, and it's a one-liner:** emit
`data-current-face="<%= currentFace %>"` on `.card-modal-overlay`. The value already reaches the
template unused. That gives the game modal its first real face observable and makes both
grouping loops verifiable. The owner identified it but deliberately didn't do it — it's a code
change, and it belongs to whoever resolves this ticket.

That reframes the question for these two tests: they may not be superfluous so much as
*currently unable to prove their premise*, which is a different and cheaper problem.

### Charting the map — the cost side is now measured, 2026-08-07

> _This was generated by AI while charting `../map.md`._

This ticket said "cost per spec plus value per spec is the whole decision" and left cost as a
query to run. It's been run — run `96588aeb`, git `e1ca060`, 104.1s total, 16 specs, 56 tests:

| spec | ms | | spec | ms |
| --- | --- | --- | --- | --- |
| **verify-query-parameter-modals** | **29,937** | | verify-prep-commander-flip | 3,319 |
| verify-mulligan | 12,112 | | verify-discard | 2,592 |
| verify-developer-mode | 9,555 | | verify-design-gallery | 2,588 |
| verify-game-menu | 9,420 | | verify-deck-title-placement | 2,194 |
| verify-library-grouping | 9,150 | | verify-history-card-links | 1,955 |
| verify-table-mode | 6,637 | | verify-prep-library-click | 1,698 |
| verify-tabletop-integration | 4,432 | | verify-precon-to-prepare | 1,695 |
| | | | verify-fleet-tokens | 1,187 |
| | | | verify-game-history | 547 |

**The evidence lands hard on the third candidate.** `verify-query-parameter-modals.spec.ts` is
**29.9s — 30% of the whole run**, nearly 2.5× the next spec. It calls setup **14 times** (the
most of any spec, out of 42 total). Nine near-identical cases over one mechanism
(`public/modal-query-params.js` reading `URLSearchParams`) is the single most expensive
thing in the suite, and it is a parameterization.

**But read this together with ticket 03.** Most of that 29.9s is 14 × 1.3s of
`/choose-any-deck` navigation — setup cost, not assertion cost. If 03 lands API-seeded setup,
this spec could drop to ~12s without deleting a single test. **That's the sequencing warning
this ticket already gave itself, now with a number on it:** a spec that looks indefensible at
29.9s may be entirely reasonable at 12s.

So the honest order is 03 first, re-measure, *then* decide here. What survives that re-measure
is a real value judgement; what doesn't was never a value judgement at all.

The two flip-loop candidates (vacuous assertion, near-duplicate loops) live in
`verify-library-grouping.spec.ts` — 9.2s, 7 setup calls. Same caveat applies, but the vacuous
assertion is wrong at *any* speed: a test that passes when the button does nothing is a useless
test regardless of cost, and the destination's first clause says so. The two-faced-cards owner's
`data-current-face` one-liner is the cheap fix, and it is not blocked on 03.

### Re-measured post-03, 2026-08-07

> _This was generated by AI while resolving this ticket._

03 landed (`seedGame()`, merged locally as `d02ba60` — not yet pushed to `origin/main`, which is
why a fresh worktree built from `origin/main` still showed the old 42-navigation cost until
merged forward from local `main`). Fresh run `94fcffb5`, warm: **56.3s total, 52.2s specs** — the
destination's 60s clause is already met, before this ticket, 06, or 11 land.

| spec | ms | | spec | ms |
| --- | --- | --- | --- | --- |
| **verify-query-parameter-modals** | **11,281** | | verify-precon-to-prepare | 1,728 |
| verify-developer-mode | 6,147 | | verify-library-command-zone-swap | 1,525 |
| verify-mulligan | 5,926 | | verify-discard | 1,478 |
| verify-game-menu | 5,282 | | verify-fleet-tokens | 1,173 |
| verify-library-grouping | 4,512 | | verify-history-card-links | 1,148 |
| verify-table-mode | 3,498 | | verify-deck-title-placement | 1,022 |
| verify-prep-commander-flip | 3,275 | | verify-prep-library-click | 729 |
| verify-design-gallery | 2,334 | | verify-game-history | 339 |

`verify-query-parameter-modals` is still the single most expensive spec — now **11.3s, 22% of
the run**, down from 29.9s/30%, and the drop is entirely setup cost leaving. What's left is 14
setup calls (still un-seeded down to one shared game per test, unlike every other spec — it
mints a fresh one per case) plus **9 assertions over one mechanism**, which is exactly the
parameterization question this ticket already asked, now with cost evidence that it's still the
top candidate even at its post-03 price.

`verify-library-grouping` (the vacuous-assertion and near-duplicate-loop candidates) is 4.5s, not
9.2s — but per this ticket's own words, the vacuous assertion "is wrong at *any* speed."

## Answer

Grilled with Jess, 2026-08-07. Three decisions:

1. **Strengthen the vacuous flip assertion.** `views/partials/card-modal.ejs` now emits
   `data-current-face="<%= currentFace %>"` on `.card-modal-overlay` (cleared by the
   two-faced-cards owner — additive, no collision, no route change needed since `currentFace`
   already reaches the template). Both flip loops in `verify-library-grouping.spec.ts` now
   assert `data-current-face` actually flips to `back`, retried with `toPass()` like every other
   click in the suite — a flip that never happened can no longer pass.
2. **Keep both flip loops** (game and prep). Confirmed load-bearing: the two pages drive the
   flip button through different mechanisms (prep `hx-get` with `?face=`, game `hx-post` to a
   toggling route).
3. **Trim `verify-query-parameter-modals.spec.ts`** from 14 browser tests to 5: `?openCard` on
   game, `?openCard` on prep, one no-params baseline, plus the two nav-arrow tests (unrelated to
   the query-param mechanism — they use `?openCard` only to get a modal open). The other 9 cases
   (`?openLibrary`, `?openTable`, `?openHistory`, `?openDebug`, and three combinations) moved to
   `test/modal-query-params.test.ts`, a new unit test that loads `public/modal-query-params.js`
   verbatim via Node's `vm` module (no jsdom dependency, no change to how the script is served)
   and exercises `planAutoOpenActions()` — a pure function extracted from the file so the
   URL-to-action decision is testable without a browser. 16 cases, ~1ms total vs. the browser
   tests they replace.

Re-measured after all three: **50.2s total, 46.1s specs** (`verify.sh` run `a2d760e6`), down from
56.3s. `verify-query-parameter-modals` dropped from 11.3s to 4.3s and is no longer the top spec.
48 Playwright tests pass (57 → 48, the 9 moved to the unit suite), plus 285 Jest unit tests
(269 → 285, +16 for `modal-query-params.test.ts`).
